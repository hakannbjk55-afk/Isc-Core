name: ci
on:
  push:
  pull_request:

jobs:
  vectors:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run vector verifier
        run: |
          set -e
          sh tools/ci_policy.sh
